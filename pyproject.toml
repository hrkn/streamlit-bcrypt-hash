[project]
name = "bcrypt-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bcrypt>=5.0.0",
    "streamlit>=1.52.2",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
